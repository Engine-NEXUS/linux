# NEXUS — no env config needed.
#
# The backend Worker URL is hardcoded in src-tauri/src/commands.rs (WORKER_URL).
# Copying this file to .env is optional and unused. Kept as a placeholder so
# tooling that expects an env file does not break.
